{
  "name": "@stockwiki/config",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('config package does not build artifacts')\"",
    "dev": "node -e \"console.log('config package has no dev process')\"",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run --passWithNoTests",
    "typecheck": "node -e \"console.log('config package has no TypeScript sources to check')\""
  }
}
